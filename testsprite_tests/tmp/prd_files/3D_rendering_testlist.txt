1. 📦 에셋 로딩 경로 (Asset Path) - 가장 유력
이유:
Expo Go: 파일들은 로컬 서버(http://localhost...)에서 실시간으로 받아옵니다.
Build: 모든 파일이 앱 안에 압축(bundled)되어 들어갑니다. 경로가 asset://resource_id 형태로 바뀝니다. 3D 엔진(C++)이 이 압축된 경로를 이해하지 못하고 **"파일 없음"**으로 처리하다가 메모리 에러(SIGSEGV)를 낼 수 있습니다.
디버깅 포인트:
Asset.fromModule() 직후의 uri 값이 실제 파일 경로(file://)인지, 리소스 식별자(asset://)인지 확인해야 합니다.
해결책: downloadAsync()를 써서 압축을 풀고 실제 파일 경로를 확보해야 합니다. (제가 방금 적용한 코드입니다!)
2. 🎮 OpenGL 컨텍스트 (GL Context)
이유: expo-gl은 네이티브 C++ 코드를 사용합니다. 배포 빌드에서는 최적화 과정에서 초기화 타이밍이 달라질 수 있습니다.
디버깅 포인트:
Step A: 복잡한 3D 모델(Three.js)을 다 빼고, 단순한 초록색 배경의 <GLView>만 띄워도 죽는지 확인합니다.
결과 해석:
단순 배경도 죽는다 → expo-gl 라이브러리 자체의 호환성/설치 문제
배경은 사는데 모델 로딩에서 죽는다 → 1번(에셋 경로) 또는 메모리 문제
3. ⚡ 자바스크립트 엔진 (JS Engine: Hermes vs JSC)
이유:
Expo Go: JSC (JavaScript Core) 엔진을 씁니다. (오래됐지만 안정적)
Build: 기본적으로 Hermes 엔진을 씁니다. (매우 빠르지만, C++ 바인딩에 엄격함)
디버깅 포인트: 
app.json
 설정을 통해 Build에서도 강제로 JSC 엔진을 쓰게 해서, 엔진 차이로 인한 크래시인지 확인합니다. (우리는 이미 JSC로 설정했습니다!)
4. 🧹 코드 난독화 (Minification/Proguard)
이유: 배포 시 코드를 압축(Minify)하는데, 3D 관련 클래스 이름이 엉뚱하게 바뀌어서 내부 호출이 끊길 수 있습니다.
디버깅 포인트: enableProguardInReleaseBuilds: false를 설정하여 난독화를 끕니다. (이것도 이미 완료했습니다!)